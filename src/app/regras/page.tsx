import { Workflow } from "lucide-react";
import { PlaceholderScreen } from "@/components/common/PlaceholderScreen";

export default function RegrasPage() {
  return <PlaceholderScreen title="Motor de Regras" icon={Workflow} />;
}
